bool test(int x, int y) {
    if x-1+y == y {
        return true;
    } else {
	return false;
    }
}


void main() {
    assert test(1,0);
    assert test(1,1);
    assert !test(0,0);
    assert !test(2,0);    
}
